
<%= simple_form_for(@admin_service) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <%= f.input :message %>
  <%= f.input :visible %>

  <div class="actions">
  <%= f.button :submit , "Grabar" %>
  </div>

<% end %>
